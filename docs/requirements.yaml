name: docs
channels:
  - conda-forge
  - psi4
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - sphinx
  - numpy
  - scipy
  - psi4

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - insegel
    - opt-einsum

name: test
channels:
  - conda-forge
  # - psi4
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pyscf
  # - psi4/label/dev::psi4
  - numpy
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - opt-einsum

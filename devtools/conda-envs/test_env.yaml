name: test
channels:
  - conda-forge
  # - psi4
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  # - psi4/label/dev::psi4
  - numpy
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - pyscf
    - opt-einsum

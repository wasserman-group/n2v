name: test
channels:
  - conda-forge
  - psi4/label/dev
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - psi4-dev
  - numpy
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - opt-einsum